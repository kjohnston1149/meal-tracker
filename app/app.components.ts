import { Component } from '@angular/core';
import { Meal } from './template.model';

@Component({
  selector: 'my-app',
  template: `
  <div class="container">
  </div>
  `
})

export class AppComponent {
}
