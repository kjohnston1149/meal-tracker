import { Component } from '@angular/core';
import { X } from './template.model';

@Component({
  selector: 'my-app',
  template: `
  <div class="container">
  </div>
  `
})

export class AppComponent {
}
